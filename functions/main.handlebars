<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;800;900&display=swap');

        *,
        *::before,
        *::after {
            box-sizing: border-box;
            margin: 0;
        }

        :root {
            --clr-dark: #000000;
            --clr-primary: #FF2F2F;
            --clr-light:  #FFFFFF;
            --clr-accent-one: #2B94FC;
            --clr-accent-two: #1D1B20;
            --clr-price: grey;
            --clr-text: #505050;
            --clr-bg: #C4C4C4;

            --fw-light: 300;
            --fw-regular: 400;
            --fw-bold: 800;
            --fw-black: 900;

            --ff-primary: 'Heebo', sans-serif;

            --letter-spacing: 0.125rem;

        }

        body {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        .email-container {
            width: 960px;
            max-width: 100%;
            margin: auto;
            background-color: #C4C4C4;
            font-family: 'Heebo', sans-serif;
        }

        .email-header {
            width: 100%;
            background-color: black;
            padding: 8px 0;
        }

        .email-container img {
            width: 112px;
            margin: auto;
        }

        .email-title {
            text-align: center;
            color: #505050 
        }

        .details-container {
            padding: 32px;
        }

        .order-details {
            width: 50%;
            min-width: 320px;
            margin-top: 64px;
            color: #505050
        }

        .order-details-list {
            padding: 0;
            list-style-type: none;
        }

        .order-details-list li {
            display: flex;
            justify-content: space-between;
            margin-bottom: 16px;
        }

        .detail-section {
            font-weight: 800;
            color: black;
        }

        .order-items {
            margin-bottom: 48px;
        }

        .order-items p {
            text-transform: capitalize;
            color: black
            font-weight: 800;
            margin-bottom: 16px;
        }

        .order-item {
            display: flex;
            align-items: center;

        }
        .order-item img {
            width: 160px;
            max-width: 100%;
        }

        .item-details ul {
            list-style-type: none;
            width: 45vw;
            min-width: 128px;
            max-width: 256px;
        }

        .item-details ul li {
            display: flex;
            justify-content: space-between;
            text-transform: capitalize;
            color: #505050;
        }

        .item-detail-field,  
        .item-detail-name{
            font-weight: 800;
            color: black;
        }

        .item-detail-name {
            color: black;
        }

        .shipping-details h1 {
            margin-bottom: 16px;
        }
        .shipping-details p {
            max-width: 640px;
            margin-bottom: 16px;
        }

    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header">
          <img src="https://firebasestorage.googleapis.com/v0/b/cheap-hype-seller.appspot.com/o/logo%2FLogo.jpg?alt=media&token=90fa8cc8-beb3-4254-a492-402ce96e3aef" alt="">
        </div>
        
        <div class="details-container">
            <h1 class="email-title">Thanks for shopping with us</h1>
            <div class="order-details">
              <ul class="order-details-list">
                <li><span class="detail-section">Items:</span>{{items.length}}</li>
                <li><span class="detail-section">Total:</span>{{total}}</li>
                <li><span class="detail-section">Shipping address:</span>{{billingDetails.address.line1}}</li>
              </ul>
            </div>

            
            <div class="order-items">
              <p>item review:</p>
                {{#each items}}

              <div class="order-items-container">
                <div class="order-item">
                  <img src={{{image}}} alt="">
                  <div class="item-details">
                    <ul>
                      <li class="item-detail-name"><span class="item-detail-field">{{name}}</span></li>
                      <li><span class="item-detail-field">price:</span>${{price}}</li>
                      <li><span class="item-detail-field">size:</span>{{size}}</li>
            
                    </ul>
                  </div>
                </div>
              </div>
            {{/each}}

            </div>

            
            <div class="shipping-details">
              <h1 class="email-title">Shipping Details</h1>
              <p>This will be e-mailed to you as soon as we ship your products, keep an eye from an email from us, for your shipping information</p>
              <p>If any deatails are wrong please email <a href="">Cheap-Hype-Seller@hype.com</a></p>
            </div>
                  </div class="email-container">
        </div>
</body>
</html>